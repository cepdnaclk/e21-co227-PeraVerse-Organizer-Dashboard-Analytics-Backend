# Connection via single DATABASE_URL (recommended)
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DB_NAME

# Or use discrete settings (used by some services)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres

# SSL controls (the overview_analytics service auto-detects)
# Precedence: DATABASE_SSL -> PGSSLMODE -> sslmode in DATABASE_URL -> default(false)
# For local Postgres without SSL
PGSSLMODE=disable
DATABASE_SSL=false

# For hosted DBs (e.g., Supabase)
# PGSSLMODE=require
# DATABASE_SSL=true
# Tip: You can also append `?sslmode=require` to DATABASE_URL
